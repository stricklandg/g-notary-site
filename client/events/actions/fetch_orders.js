import { Tracker } from 'meteor/tracker';
import Orders from '../../../lib/collections/orders';


//export default function fetchOrder() {
 //   return dispatch => {
 //       console.log("fetchOrder warming up...");
  //      Tracker.autorun(() => {
  //          console.log("fetchOrder's tracker is now running");
   //         dispatch({
   //             type: 'FETCH_ORDERS',
   //             orders: Orders.find().fetch(),
  //          });
  //      });
  //  }
// };